import 'package:flutter/material.dart';

class Otherpage extends StatelessWidget {
  const Otherpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Other",style: TextStyle(fontSize: 28),),
      ),
    );
  }
}